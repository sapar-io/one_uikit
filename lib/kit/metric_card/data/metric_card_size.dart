part of '../one_metric_card.dart';

/// Metric card size (affects padding and spacing)
enum MetricCardSize {
  /// Mobile size (smaller padding)
  mobile,

  /// Desktop size (larger padding)
  desktop,
}
